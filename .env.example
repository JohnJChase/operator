# Copy to .env only when a phase needs these values.
# Do not commit .env.

# Phase 5+: news refresh (weather/Open-Meteo needs no key).
NEWSDATA_API_KEY=

OPENAI_API_KEY=
TELNYX_API_KEY=
TELNYX_CONNECTION_ID=

# Optional future local diagnostics console.
OPERATOR_CONSOLE_PASSWORD=
